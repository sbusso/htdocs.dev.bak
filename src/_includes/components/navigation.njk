<nav class="flex flex-wrap items-center justify-between py-3 border-b">

    <a href="/" class="text-2xl font-semibold text-gray-900 font-poppins">htdocs<span class="text-gray-400">.dev</span></a>

    <ul>
        {% for entry in metadata.navigation %}
            <li class="inline">
                <a
         href="{{ entry.url }}"
         class="uppercase text-sm py-1 px-2 rounded mr-4 inline-block
         {{'bg-black text-white' if entry.url in page.url
         else 'text-gray-700 hover:text-gray-900'}}">
                    {{ entry.title }}
                </a>
            </li>
        {% endfor %}
    </ul>

</nav>